
export const createGame = async (
    betAmount,
    betToken,
    betTokenId,
    betTokenType,
    betTokenDecimal,
    gameID,
  ) => {

    try{
    
    } catch (error) {
      // console.log(error);
      return {
        success: false,
        error,
      };
    }
  };

  export const joinGame = async (
    betAmount,
    betToken,
    betTokenId,
    betTokenType,
    betTokenDecimal,
    gameID,
  ) => {

    try{
   
    } catch (error) {
      // console.log(error);
      return {
        success: false,
        error,
      };
    }
  };

  export const removeGame = async (
    gameID
  ) => {

    try{
    
    
    } catch (error) {
      // console.log(error);
      return {
        success: false,
        error,
      };
    }
  };

